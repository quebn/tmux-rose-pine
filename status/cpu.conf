# vim:set ft=tmux:
%hidden MODULE_NAME="cpu"

set -ogq "@rosepine_${MODULE_NAME}_icon" " "
set -ogq "@rosepine_${MODULE_NAME}_color" "#{cpu_bg_color}"
set -ogq "@rosepine_${MODULE_NAME}_text" "#{cpu_percentage}"

source -F "#{d:current_file}/../utils/status_module.conf"
