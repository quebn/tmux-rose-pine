# vim:set ft=tmux:
%hidden MODULE_NAME="weather"

# Requires https://github.com/xamut/tmux-weather.

set -ogq @rosepine_weather_icon " "
set -ogqF @rosepine_weather_color "#{@thm_gold}"
set -ogq @rosepine_weather_text "#{weather}"

source -F "#{d:current_file}/../utils/status_module.conf"
